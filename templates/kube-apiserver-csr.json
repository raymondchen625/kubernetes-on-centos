{
  "CN": "kubernetes",
  "key": {
    "algo": "rsa",
    "size": 2048
  },
  "names": [{
    "C": "${CERT_C}",
    "L": "${CERT_L}",
    "O": "${CERT_CN}",
    "OU": "${CLUSTER_NAME}",
    "ST": "${CERT_ST}"
  }]
}
