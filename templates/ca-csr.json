{
  "CN": "${CERT_CN}",
  "key": {
    "algo": "rsa",
    "size": 2048
  },
  "names": [{
    "C": "${CERT_C}",
    "L": "${CERT_L}",
    "O": "${CERT_CN}",
    "OU": "${CERT_OU}",
    "ST": "${CERT_ST}"
  }]
}
